/* export var HandleText = ev => {
    var obj;
    if (ev.target.value !== "") {
    obj = { valid:true, class:"valid", value:ev.target.value}
  }
    else {
      obj = { valid:false, class:"invalid", value:ev.target.value }
    }
      return obj;
} */